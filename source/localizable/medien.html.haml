---
id: media-coverage
---

- content_for :header do
  %h1
    = page_title

%section
  .container
    - config[:media_coverage].sort_by { |media_coverage| media_coverage.fetch(:date) }.reverse.each do |media_coverage|
      .row
        .col-sm-8.col-sm-offset-2
          .item
            = image_tag "media-coverage/#{media_coverage.fetch(:outlet)}.png"
            %p
              == "#{media_coverage.fetch(:text)}"
            = I18n.l media_coverage.fetch(:date), format: :long
            &middot;
            = link_to 'Link', media_coverage.fetch(:link), rel: 'nofollow', target: '_blank'
